[project]
name = "mcp-gemini-demo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "pengux", email = "roninguyen@icloud.com" }
]
requires-python = ">=3.14"
dependencies = [
    "google-genai>=2.23.0",
    "mcp[cli]>=2.2.0",
]

[project.scripts]
mcp-gemini-demo = "mcp_gemini_demo:main"

[build-system]
requires = ["uv_build>=0.12.13,<0.13.0"]
build-backend = "uv_build"
